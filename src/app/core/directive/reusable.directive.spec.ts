import { ReusableDirective } from './reusable.directive';

describe('ReusableDirective', () => {
  it('should create an instance', () => {
    const directive = new ReusableDirective();
    expect(directive).toBeTruthy();
  });
});
